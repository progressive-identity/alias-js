version: "2"

services:
    authz.gdpr.dev.local:
        image: alias/sandbox
        ports:
            - "127.0.1.2:80:8080"
        command: "sh -c \"(cd provider && make run)\""
        volumes:
            - ../:/alias

    client.gdpr.dev.local:
        image: alias/sandbox
        ports:
            - "127.0.1.1:80:8080"
        command: "sh -c \"(cd client && make run)\""
        volumes:
            - ../:/alias

    processor.authz.gdpr.dev.local:
        image: alias/sandbox
        ports:
            - "127.0.1.3:80:8080"
        command: "sh -c \"(cd processor/daemon && make run)\""
        volumes:
            - ../:/alias

